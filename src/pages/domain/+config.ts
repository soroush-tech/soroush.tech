import type { Config } from 'vike/types'

export default {
  title: 'Delivery Domains',
  description:
    'From data model to deployment, frontend to infrastructure, AI to sub-second performance — where complex systems get architected, scaled, and shipped under load.',
} satisfies Config
