# Examples are standalone projects, installed independently of the monorepo.
# Their installed deps and lockfiles are never tracked here.

# Installed deps / build output
**/node_modules/
**/build/
**/dist/
**/coverage

# Lockfiles — every package manager
**/package-lock.json
**/npm-shrinkwrap.json
**/pnpm-lock.yaml
**/yarn.lock
**/bun.lockb
**/bun.lock
**/deno.lock
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
